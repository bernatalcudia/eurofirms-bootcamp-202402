// usando el map crea una array multiplicada por 2 case 1


var numbers = [10, 20, 30, 40, 50, 60]


var case1 = numbers.map(function (x) {
    return x * 2
})

console.log(result)

//crea un nuevo array con la longitud de cada uno de los strings casw 2

var strings = ['hola', 'mundo', 'a', 'todos']

var case2 = strings.map(function (x) {
    var result = {}

    var caracterCount
})


